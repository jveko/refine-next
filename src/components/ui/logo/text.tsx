import { Typography } from "antd";

export const LogoText = (props: React.SVGProps<SVGSVGElement>) => (
  <Typography.Title level={3} style={{ margin: 0, color: "#185FA3" }}>
    Templates
  </Typography.Title>
);
